generator client {
	provider = "prisma-client-js"
}


datasource db {
	provider = "mysql"
} 

model Filme {
	id          Int    @id @default(autoincrement())
	title       String
	director    String
	releaseYear Int
	genre       String
	rating      Float
	synopsis    String
}